More option & parameters available.

import preconstructed directory which deliver usable "program as services" and provide more options and parameters.
for example, we directly establish a connection instead of create it because it is done by the program in its dedicated directory and configurable
hello world: is more sequential and use mostly native commands.
Break repos  : This organization will  bring a better visibility of  the code and centralize management of the configuration for each needed services available in its dedicated directory.
